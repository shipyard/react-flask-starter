version: '3'

services:

  frontend:
    labels:
      shipyard.route: '/'
    build: 'frontend'
    environment:
      CI: 'true'
      DANGEROUSLY_DISABLE_HOST_CHECK: 'true'
    env_file:
      - frontend/frontend.env
    volumes:
      - './frontend/src:/app/src'
      - './frontend/public:/app/public'
    ports:
      - '3000:3000'

  py-backend:
    labels:
      shipyard.route: '/api'
    build:
      context: 'backend/python'
    environment:
      DATABASE_URL: 'postgres://obscure-user:obscure-password@postgres/app'
      DEV: ${DEV}
      FLASK_DEBUG: '1'
      LOCALSTACK_HOST: 'localstack'
    volumes:
      - './backend/python/filesystem/entrypoints:/entrypoints:ro'
      - './backend/python/migrations:/srv/migrations'
      - './backend/python/src:/srv/src:ro'
    ports:
      - '8080:8080'
  go-backend:
    labels:
      shipyard.route: '/api/gif-search'
    build:
      context: '.'
      dockerfile: backend/go/Dockerfile
    environment:
      DATABASE_URL: 'postgres://obscure-user:obscure-password@postgres/app'
      WEB_SERVICE_PORT: 8081
      GIPHY_API_KEY: ${GIPHY_API_KEY}
      FILE_UPLOAD_ENDPOINT: "http://py-backend:8080/api/v1/files/upload/"
    ports:
      - '8081:8081'
  worker:
    labels:
      shipyard.init: 'poetry run flask db upgrade'
    build:
      context: 'backend/python'
    environment:
      DATABASE_URL: 'postgres://obscure-user:obscure-password@postgres/app'
      DEV: ${DEV}
      FLASK_DEBUG: '1'
      LOCALSTACK_HOST: 'localstack'
    command: '/entrypoints/worker.sh'
    volumes:
      - './backend/python/filesystem/entrypoints:/entrypoints:ro'
      - './backend/python/migrations:/srv/migrations'
      - './backend/python/src:/srv/src:ro'

  postgres:
    image: 'postgres:9.6-alpine'
    environment:
      POSTGRES_USER: 'obscure-user'
      POSTGRES_PASSWORD: 'obscure-password'
      POSTGRES_DB: 'app'
      PGDATA: '/var/lib/postgresql/data/pgdata'
    volumes:
      - 'postgres:/var/lib/postgresql/data'
    ports:
      - '5432'

  redis:
    image: 'redis:5.0-alpine'
    ports:
      - '6379'

  localstack:
    image: 'localstack/localstack'
    environment:
      - DATA_DIR=/tmp/localstack/data
    volumes:
      - 'localstack:/tmp/localstack'
    ports:
      - '4566:4566'

volumes:
  postgres:
  localstack:
